{
  "framework": "nextjs",
  "buildCommand": "rm -rf .next .swc node_modules/.cache && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/refresh",
      "schedule": "30 12 * * *"
    }
  ]
}
